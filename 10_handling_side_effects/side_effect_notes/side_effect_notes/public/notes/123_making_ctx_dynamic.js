// we can not only pass data through ctx
// but can also pass functions

// example from the app
{/* <AuthContext.Provider value={{
  isLoggedIn: isLoggedIn,
  onLogout: logoutHandler,
}}> */}

// onLogout points to the logoutHandler funct in App.js
