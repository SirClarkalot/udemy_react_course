// Another way to consumer the ctx is with
// the useContext hook

// instead of .Consumer
// Fist--> import React, {useContext} from 'react';
//  second--> use the hook in your component and set to a variable
//   example: const ctx = useContext(AuthContext);
